<Window x:Class="D201_Assignment_01.ManageUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:D201_Assignment_01"
        mc:Ignorable="d"
        Title="Manage Users" Height="400" Width="500">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="*"></RowDefinition>
    </Grid.RowDefinitions>

    <!-- Toolbar -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
      <Button Content="Add User" Click="AddUserButton_Click" Margin="0,0,5,0" Padding="8,4"/>
      <Button Content="Remove User" Click="RemoveUserButton_Click" Margin="0,0,5,0" Padding="8,4"/>
    </StackPanel>

    <ListView Grid.Row="1" x:Name="listViewUsers">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="First Name" DisplayMemberBinding="{Binding FirstName}" Width="120"/>
          <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding LastName}" Width="120"/>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</Window>
