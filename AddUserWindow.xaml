<Window x:Class="D201_Assignment_01.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:D201_Assignment_01"
        mc:Ignorable="d"
        Title="AddUserWindow" Height="450" Width="800">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- First Name -->
    <Label Grid.Row="0" Grid.Column="0" Content="First Name:"/>
    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtFirstName" Margin="5"/>

    <!-- Last Name -->
    <Label Grid.Row="1" Grid.Column="0" Content="Last Name:"/>
    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtLastName" Margin="5"/>

    <!-- Buttons -->
    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Add User" Click="AddUserButton_Click" Margin="5" Width="80"/>
      <Button Content="Cancel" Click="CancelButton_Click" Margin="5" Width="80"/>
    </StackPanel>
  </Grid>
</Window>
